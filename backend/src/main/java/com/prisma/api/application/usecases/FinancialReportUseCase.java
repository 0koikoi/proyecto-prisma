package com.prisma.api.application.usecases;

import java.math.BigDecimal;
import java.time.LocalDate;
import java.util.List;

public interface FinancialReportUseCase {

    // FinancialSummaryDto getSummary(LocalDate from, LocalDate to);

    // List<FinancialTransactionDto> getTransactions(LocalDate from, LocalDate to, String type);
}
